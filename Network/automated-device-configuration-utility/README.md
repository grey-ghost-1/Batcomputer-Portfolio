# Automated Device Configuration Utility

Generates a reviewable, plan-only configuration payload for authorized devices. It does not connect to or modify network equipment. Run `python main.py targets.json --command 'vlan 20'`.
