# Network Monitoring Dashboard

A runnable in-memory telemetry model for device uptime, latency, bandwidth, and alert summaries.

Run `python main.py` to generate a JSON dashboard snapshot. A production adapter can later connect the model to SNMP polling.
