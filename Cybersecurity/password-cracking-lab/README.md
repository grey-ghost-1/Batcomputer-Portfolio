# Password Cracking Lab

A local educational hash comparison demo. It generates a sample digest in memory and checks a tiny training wordlist. It never targets accounts, services, or third-party hashes.

Run `python main.py`.
